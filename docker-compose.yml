services:
  lingoloop:
    image: lingoloop:production
    pull_policy: never
    build:
      context: .
      dockerfile: Dockerfile
    container_name: lingoloop
    restart: unless-stopped
    ports:
      - "127.0.0.1:8080:80"